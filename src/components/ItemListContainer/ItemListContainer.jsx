const ItemListContainer = (props) => {
    return(
        <div>
        <h1 style={{ display:'flex', justifyContent:'center', background:'pink'}}>{props.saludo}</h1>
        </div>
    )
    
}
export default ItemListContainer