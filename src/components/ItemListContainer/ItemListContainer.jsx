const ItemListContainer = (props) => {
    return(
        <div>
        <h1 style={{ fontSize: 30, display:'flex', justifyContent:'center', background:'pink'}}>{props.saludo}</h1>
        </div>
    )
    
}
export default ItemListContainer